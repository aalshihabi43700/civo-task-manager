name: CI CD + Certificate Monitoring

on:
  push:
    branches:
      - main

  workflow_dispatch:

  schedule:
    - cron: "0 6 * * *"

permissions:
  contents: write

jobs:

  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Build backend
      # Trivy
      # Push backend
      # Build frontend
      # Trivy
      # Push frontend
      # Update Helm

  certificate-monitor:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure kubeconfig
        shell: bash
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_CIVO }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Verify Kubernetes connection
        run: kubectl get nodes

      - name: Check certificate expiry
        shell: pwsh
        run: ./scripts/check-cert-expiry.ps1