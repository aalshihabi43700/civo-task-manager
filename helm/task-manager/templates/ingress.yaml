{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "task.fullname" . }}
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-stg-task
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend-stg-task
                port:
                  number: 8000
{{- end }}